var http = require('http');
var assert = require('assert');
var server = require('./server.js');

describe("Opening test", function(){
  before(function(){
    server.listen(8989);
  })

  after(function() {
    server.close();
  })

  describe('test', function() {
    it('Main should return ok', funciton(res) {
      assert.equal(res.statusCode, 200)
    })
  })
})
